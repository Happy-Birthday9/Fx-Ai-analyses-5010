TRADING STRATEGY VAULT — GROQ ONLY FINAL

Groq Qwen 3.6 27B Vision is used for chart screenshot analysis.

Enter the Groq API key from the website's 🔐 API Settings. No real key is included in this ZIP.
The key is stored in this browser/device localStorage.

This build fixes the previous JavaScript syntax/runtime issue that caused most buttons to stop working. Color Change, AI Analysis, Future Signals, Money Management, install, admin, strategy cards and API settings are wired.

Security note: browser-side API keys are not fully secret on public GitHub Pages. Use a rotatable/restricted key.
